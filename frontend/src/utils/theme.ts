import { extendTheme } from "@chakra-ui/react";

const Theme = extendTheme({
    colors: {
        brand: {
            100: '#2A0B42', //Purple
            300: '#DAD7DB', //White
            500: '#F8CD51',
        },
    }
});

export default Theme;
